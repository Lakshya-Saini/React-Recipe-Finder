import React from "react";
import { Link, NavLink } from "react-router-dom";

const Navbar = () => {
  return (
    <nav className="navbar navbar-primary bg-primary pb-2 mb-5">
      <span className="navbar-brand text-white mx-auto">Recipe App</span>
    </nav>
  );
};

export default Navbar;
